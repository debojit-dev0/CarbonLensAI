# NVIDIA AI (server-side only — never expose in client code)
NVIDIA_API_KEY=
NVIDIA_MODEL=meta/llama-3.1-70b-instruct
NVIDIA_BASE_URL=https://integrate.api.nvidia.com/v1

# Firebase Realtime Database (server-side admin credentials)
FIREBASE_DATABASE_URL=
FIREBASE_PROJECT_ID=
FIREBASE_CLIENT_EMAIL=
FIREBASE_PRIVATE_KEY=

# Public, non-secret flags only
NEXT_PUBLIC_APP_NAME=CarbonLens AI
